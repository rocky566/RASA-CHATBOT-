version: "2.0"

rules:
  - rule: Say hello when greeted
    steps:
      - intent: greet
      - action: utter_greet

  - rule: Provide the current date
    steps:
      - intent: ask_date
      - action: action_show_date
